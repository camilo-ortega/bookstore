import 'package:flutter/material.dart';

class BookDetailModelView extends StatelessWidget {
  const BookDetailModelView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
