import 'package:flutter/material.dart';

class SearchBookModelView extends StatelessWidget {
  const SearchBookModelView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
