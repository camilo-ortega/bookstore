import 'package:flutter/material.dart';

class ListBookModelView extends StatelessWidget {
  const ListBookModelView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
